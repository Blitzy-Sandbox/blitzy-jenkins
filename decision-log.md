# Decision Log — Acceleration Report Generation

## Overview

This decision log is the single source of truth for the rationale behind every non-trivial methodological choice made while generating `acceleration-report.md` and its companion executive presentation `acceleration-report.html`. Each row records what was decided, what alternatives were considered, why the chosen alternative was adopted, and what risks that choice carries. The log is mandated by the project-level Explainability rule; it is paired with a bidirectional traceability matrix that maps every Decision ID to the report sections it governs and every major report section to the Decision IDs applied in it.

---

## Decisions

| ID | Decision | Alternatives Considered | Why This Choice | Risks |
|----|----------|--------------------------|------------------|-------|
| D001 | Use the earliest `Co-authored-by: Copilot` trailer as the Tool Introduction Date. Anchor: `2025-06-19T11:03:50+01:00` (commit `c701361ec7bc9aa58d7745de6291a01b3d7abbe4`, subject `Fix Typo`). | A1: Earliest `agent@blitzy.com` direct-author commit (`2026-02-19T10:42:03+00:00`, `f0f89db8ec0ef59109dc496e6cbbf8a5439032ab`, 154 total such commits). A2: Inflection-point detection on monthly non-merge commit velocity (command R1.3; no specific anchor date and Low confidence). A3: Earliest commit by any author-email matching `@blitzy.com` or any AI-identifier pattern (produces the same `2026-02-19` anchor as A1 plus additional human Blitzy contributors such as `michael@blitzy.com`, which biases toward the later anchor). | The Copilot co-author trailer is the earliest verifiable AI co-authoring signal present in the repository and predates the `agent@blitzy.com` signal by 245 days. Choosing it yields a 292-day After window rather than a 46-day window, which preserves the Ramp-Up / Steady-State distinction and keeps D010 untriggered. Primary-signal ordering also follows the user prompt, which lists `Copilot` first among AI-tool identifiers (`Copilot`, `Claude`, `Cursor`, `Windsurf`, `Cody`, `blitzy`, `anthropic`, `openai`). The signal is a Medium-confidence heuristic (pattern match on commit-trailer text). | The Copilot co-author trailer appears on only 5 commits, while `agent@blitzy.com` authors 154 commits; the After-period activity mix is dominated by the later-arriving Blitzy Agent workflow rather than by Copilot-assisted commits. Per-activity After-period multipliers are therefore driven mainly by a tool cohort that began operating 245 days after the anchor. The Risk Assessment section (§8.3) of `acceleration-report.md` surfaces the anchor-choice ambiguity and lists the alternative anchor explicitly. |
| D002 | Exclude bot-author identities (`renovate[bot]`, `dependabot[bot]`, `github-actions[bot]`, `jenkins-release-bot`, `release-bot`) from Activities 3 (Code Generation), 6 (Defect Response), and 7 (Commit Throughput). Retain them with callout in Activities 1, 2, 4, 5. For Activity 6, additionally report the unfiltered ratio to expose the confounder effect. | A1: Exclude bots from all 7 activities uniformly. A2: Include bots in all 7 activities without callout. A3: Include bots in Activities 1–5 and exclude from 3, 6, 7 (same partition but without the Activity 6 unfiltered-ratio dual report). | Bot authors generate commits at cadences unrelated to human engineering velocity; including them in commit-centric activities (3, 6, 7) inflates Baseline and After counts without reflecting real output. Bots rarely create feature branches, test files, documentation, or architecture docs, so their inclusion in Activities 1, 2, 4, 5 is informational rather than distortive. Reporting the Activity 6 unfiltered ratio in addition to the filtered one preserves Rule 5 parity and makes the confounder visible rather than masked. | A service account that does not carry the `[bot]` suffix (or `jenkins-release-bot` / `release-bot` literal) is not filtered. Legitimate human-authored output surfaced via an automated pipeline (e.g., bot-forwarded PR merges) is excluded. Both residual risks are documented in `acceleration-report.md` §8.3 (Confounding Factors). |
| D003 | Align every 2-week bucket to start on a Monday at `00:00:00+00:00`. Anchor: the Monday of the week containing the Tool Introduction Date (`2025-06-16T00:00:00+00:00`). Every bucket is a half-open interval `[anchor + 14k days, anchor + 14(k+1) days)` for integer `k`. | A1: ISO-8601 default week alignment (Monday-start; equivalent to the chosen option). A2: Sunday-start alignment (US convention). A3: Anchor-based buckets pinned to the Tool Introduction Date itself (non-Monday anchor). | The user prompt explicitly specifies Monday-start 2-week window alignment. Deriving the anchor from the Tool Introduction Date Monday (rather than an arbitrary calendar Monday) keeps Baseline and After buckets on the same integer offsets, which is required by Rule 5 (identical window alignment before and after). Anchor-based buckets pinned to the exact Tool Introduction Date would produce non-Monday boundaries and break the prompt's alignment requirement. | The earliest and latest Baseline buckets and the final After bucket can be partial (fewer than 14 days observed). Partial buckets are counted at their observed event counts, producing small edge-of-window attribution noise. The effect is symmetric across Baseline and After and does not favor one partition. |
| D004 | Activity 3 (Code Generation) counts merge commits only via `git log master --merges --first-parent`. Activity 7 (Commit Throughput) counts non-merge commits only via `git log --all --no-merges`. Activities 1, 2, 4, 5 operate on their own file-creation or branch-signal commands. Activity 6 operates on `--no-merges` subjects only. | A1: Include all commits (merge and non-merge) in both Activities 3 and 7. A2: Exclude merge commits uniformly from all 7 activities. | The user's activity table defines Activity 3 explicitly as "Merge commits per 2-week window" and Activity 7 explicitly as "Non-merge commits per active engineer". Treating them symmetrically would double-count integrated work — a merge commit and its merged-in non-merge commits describe the same unit of work through two different lenses. A2 would erase Activity 3 entirely and would not match the user's table. | Squash-merge pull requests produce a single non-merge commit on `master` authored by the original contributor; such commits count in Activity 7 but not Activity 3. If the repository's merge policy shifted from `--no-ff` merge commits to squash-merges during the observed window, Activity 3 counts for that window are biased low and Activity 7 counts are biased high. The confounder is documented in `acceleration-report.md` §3.6 and §8.3. |
| D005 | Activity 1 primary heuristic — count surviving refs matching `^(feature\|feat)/` or `^JENKINS-[0-9]+` via `git for-each-ref refs/heads refs/remotes`. Fallback heuristic — if fewer than 10 surviving matching refs exist, count merge commits on `master` whose merged-in branch has at least 3 non-merge commits. At extraction time the primary heuristic returned zero surviving refs, so the fallback is applied. | A1: Primary only — accept zero if the naming convention yields none. A2: Fallback only — ignore branch-naming signal entirely regardless of repository conventions. A3: Use GitHub PR titles as a proxy for feature branches. | Jenkins core uses heterogeneous branch naming (`feature/`, `JENKINS-<NNN>`, ad-hoc author-prefixed branches, and others); relying on a single naming pattern produces false-negatives in this repository. The fallback rule observes merged-in work regardless of branch name and is stable across naming convention changes. The 10-ref threshold bounds the false-positive risk of applying the fallback to repositories where the primary signal is adequate. A3 requires external-system access and is prohibited by the read-only boundary. | Branches that were merged and deleted before extraction are not directly observable; the fallback captures them only via the surviving merge commit on `master`, and only when the merged-in branch had ≥3 non-merge commits. Small-but-legitimate feature branches (single-commit fixes, renames) are missed. Conversely, the fallback over-counts bulk merges that are not feature-bearing (routine upgrades, branch reconciliations). Both effects are surfaced as Medium-confidence boundary conditions in `acceleration-report.md` §4.1. |
| D006 | Match `ARCHITECTURE.md`, `tech-spec*`, `adr-*`, and `design-*` at ANY depth via the git pathspec `:(glob)**/<pattern>` and **do not** include `docs/technical-specifications.md` under a relaxed interpretation. Render the result literally as `Insufficient signal — no files matched ARCHITECTURE.md, tech-spec*, adr-*, or design-* patterns across the repository lifetime` per Rule 2 when the pattern set returns zero. | A1: Literal match only (no glob), root-level only — would produce zero matches. A2: Broader glob including `*.md` under `docs/architecture/`, `docs/adr/`, `docs/design/`, or prefix `technical-*`. A3: Render `Insufficient signal` without attempting the glob interpretation. | The user's activity table specifies the pattern set literally: `ARCHITECTURE.md, tech-spec*, adr-, design-`. The glob `**/<pattern>` preserves these tokens at any depth and is the minimum change required to make the pattern applicable to subdirectory layouts. Relaxing to include `docs/technical-specifications.md` (A2) is inconsistent with Rule 2's literalness requirement; if adopted, Activity 2 would yield 1 After creation and 0 Baseline creations, which is already noted as an alternative scenario in `acceleration-report.md` §4.2 boundary conditions. Preserving the strict pattern and reporting the Insufficient-signal string honors Rule 2 without distorting Activity 2 with a proxy metric. | The strict pattern misses design activity recorded in adjacent files such as `docs/technical-specifications.md` and `docs/project-guide.md`; these files are counted under Activity 5 (Documentation) instead. Activity 2 therefore under-represents design activity when measured as a proxy for design thinking rather than as a proxy for design-document creation under the specific pattern set. The boundary condition is documented in `acceleration-report.md` §4.2 and §9.2. |
| D007 | Scan commit subject lines only (`git log --format='%s'`) using a case-insensitive word-boundary regex `\b(fix\|bugfix\|hotfix\|revert)\b`. Operate on `--no-merges` output so the denominator matches Activity 6 scope. A `revert` commit counts once (as a fix-pattern match); it does not double-count as both `fix` and `revert` because the regex uses a single alternation match per subject. | A1: Scan the full commit-message body (`%B`) for the same keywords. A2: Scan subjects only with case-sensitive regex. A3: Include additional keywords such as `patch`, `repair`, `correct`, `resolve`. A4: Count `revert` and `fix` separately, producing a two-axis count per commit. | Jenkins contributors record defect-resolution intent in subject lines; body-scanning (A1) inflates false-positives from keywords embedded in quoted source, PR discussions, or dependency-upgrade descriptions. Word-boundary anchoring prevents matches on `prefix`, `affixed`, `fix.c` paths, and similar non-defect strings. Case-insensitivity catches `Fix`, `FIX`, `fix` uniformly. The 4-keyword set matches the user prompt's specification; extending it (A3) requires a Rule 5 parity re-run of Baseline and introduces non-uniformity. Counting a commit once per match (not once per keyword) preserves the single-commit-single-fix semantics of the Defect Response measure. | Fix commits whose subjects use non-matching vocabulary (e.g., "Correct typo", "Patch CSRF flaw", "Resolve NPE in agent registration") are missed. If team vocabulary shifted between Baseline and After, the bias direction is asymmetric. `acceleration-report.md` §4.6 and §8.3 document both false-positive and false-negative cases. |
| D008 | Sort non-bot human authors by **After-period** non-merge commit volume descending; assign `Engineer A` to the highest-volume After-period author, `Engineer B` to the second, and so on. Mapping generated deterministically from the output of `git shortlog -sne --no-merges --since=2025-06-19` filtered to exclude bot authors. Mapping is regenerable by re-running R9.2 against the same repository state and applying D002 bot exclusions. | A1: Sort by **total lifetime** non-merge commit volume descending (changes the rank order materially because one high-Baseline-volume author might lead the lifetime ranking without meaningful After activity). A2: Sort by Activity-3 merge volume descending (biases toward maintainers who perform merges but may not author code). A3: Sort by earliest-commit date ascending (loses productivity signal). A4: Random permutation seeded by a constant (defeats deterministic regeneration). | Per-engineer Activity 7 (§6.2) and Activity 3 (§6.1) tables in `acceleration-report.md` are ordered by After-period volume, so the pseudonym assignment matches the ranking visible in the report. Using After-period volume isolates post-Tool-Introduction productivity, which is the subject of the report's headline multipliers. The rule is deterministic and reproducible by re-running the documented command against the same ref graph. | A first-time contributor with zero Baseline commits receives Engineer A if their After-period volume leads the ranking (as occurs in this repository with 154 non-merge commits). Their Activity 7 multiplier is therefore rendered `Insufficient signal — no Baseline commits (division by zero)` per D009, which identifies their Engineer ID by elimination to any reader who knows who joined the repository most recently. Mitigated by reporting only range and median over engineers with a valid Baseline denominator, and by presenting raw per-engineer rows only where Quality Gate 5 requires them. |
| D009 | Render insufficient data as the literal string `Insufficient signal — [specific reason]` (em-dash followed by a single space followed by a bracketed reason phrase). Every instance is recorded in the Requirements Traceability Matrix (§5) with `Status = Insufficient signal` and a Deviation Reference pointing to §8 (Risk Assessment). When the Baseline or After leg of an activity is Insufficient, the Multiplier cell is also rendered with the same literal string. | A1: Render `N/A` without a bracketed reason. A2: Render `0` with a footnote. A3: Omit the cell entirely. A4: Render `—` (em-dash alone). | The user's Rule 2 mandates the exact phrasing `Insufficient signal — [specific reason]`. A1 obscures the reason and produces orphan traceability rows. A2 creates internal inconsistency because `0` is a valid extraction result that is structurally different from insufficient evidence. A3 leaves orphan rows in the Traceability Matrix and violates Rule 1. A4 matches the decision-log's internal convention for empty cells but is not distinguishable from a legitimate missing reference. | If a single activity renders Insufficient signal in Baseline but not in After (or vice versa), the Multiplier cannot be computed numerically; the Acceleration Curve row is marked with the same literal string and the Risk Assessment entry flags it. The literal string contains the em-dash character (`—`, U+2014), which can be mangled by non-UTF-8 terminals; the report is encoded as UTF-8 and the grep verifier uses the literal character. |
| D010 | If fewer than 90 days of post-introduction data exist between the Tool Introduction Date and the latest commit, collapse the temporal segmentation into Baseline vs. Post-Introduction only (omit Ramp-Up / Steady State). At extraction time the observed post-introduction window is 292 days (latest commit `2026-04-06T23:09:56-04:00`), so D010 is **not triggered**; Ramp-Up and Steady State are reported as distinct phases in §7. | A1: Keep Ramp-Up as whatever days exist and leave Steady State empty. A2: Extend the Ramp-Up definition to cover whatever window is available (alter the user's 90-day definition). A3: Skip the Temporal phases entirely when fewer than 90 days exist. | The user's temporal-phase table defines Ramp-Up as "first 90 days post-introduction"; a Ramp-Up shorter than 90 days cannot be populated under that definition. A1 leaves Steady State empty, which would violate Gate 6 (populate or justify N/A). A2 unilaterally redefines user-provided phase boundaries. A3 discards all post-introduction information. Collapsing to Baseline vs. Post-Introduction preserves all observed events while honoring the user's definition. The alternative Tool Introduction Date under D001.A1 (`2026-02-19`) would trigger D010, which is why the anchor choice and this collapse rule are linked. | Collapsed reports lose the Ramp-Up-versus-Steady-State contrast, which is a primary output of the Acceleration Curve. This is noted in `acceleration-report.md` §9.1 (Data Gaps) and §9.3 (What This Analysis Cannot Determine). D010 is not triggered under the D001 primary-signal choice, so this risk is latent rather than active in the current report. |
| D011 | Target the `master` branch (not `main`) for Activity 3 (`--first-parent`) and Activity 7 queries. Reconnaissance and extraction-time `git branch -a` show `master` as the repository's primary branch; no rename to `main` is detected in the observed ref graph. | A1: Target `main` and accept whatever Git reports (would yield zero merges because `main` does not exist in this repository). A2: Detect the primary branch dynamically per-query via `git symbolic-ref refs/remotes/origin/HEAD` and substitute the detected name. A3: Target both `master` and `main` via a union (double-counts if both exist during a rename window). | The repository uses `master` as its primary branch across the observed history; hard-coding `master` in the per-activity commands is the simplest form that matches the observed ref graph. A1 fails against this repository entirely. A2 is more portable but introduces an extra extraction step that risks silent drift between runs if the symbolic ref is ambiguous. A3 over-counts during rename windows. The primary-branch name is surfaced in the report's Methodology §3.6 so a reviewer auditing the result against a different repository can substitute the correct branch name. | If a rename from `master` to `main` occurred in a period of history not covered by extraction, Activity 3 merges prior to the rename would be missed by a literal `master` query. Inspection of the current ref graph does not show evidence of such a rename. Documented in `acceleration-report.md` §3.6 as a low-severity confounder. |
| D012 | Report both bot-filtered and unfiltered fix-commit ratios for Activity 6 (Defect Response). The bot-filtered ratio (non-bot numerator / non-bot denominator) is the primary figure; the unfiltered ratio (all-author numerator / all-author denominator) is disclosed as supplementary in §4.6 and §7 to expose the confounder effect. | A1: Report the bot-filtered ratio only and relegate the unfiltered value to an appendix note. A2: Report the unfiltered ratio only (treats bots as legitimate contributors to defect work). A3: Report a single "hybrid" ratio that excludes bots from the numerator but includes them in the denominator. | Activity 6 is a ratio measure, and a shift in the bot share of commits moves the ratio even when human fix behavior is unchanged. Reporting both values exposes the confounder rather than hiding it under one filtering policy. A3 is mathematically mixed-scope and does not describe a coherent population of commits. The dual-reporting preserves Rule 5 parity (identical methodology applied twice with and without the bot filter) and surfaces that the two filtering policies point in opposite directions for the After-period (non-bot ratio 1.48× Baseline, unfiltered ratio 0.79× Baseline). | Dual reporting doubles the Activity 6 row count in the Acceleration Curve and Risk Assessment and invites the reader to prefer one figure over the other by intuition rather than by methodology. Both rows are tagged Medium confidence to prevent implicit ranking. |
| D013 | Report both weighted and simple-mean per-engineer-per-2-week rates for Activity 7. Weighted rate (total non-merge commits ÷ total engineer-bucket pairs) is the primary figure; simple-mean rate (mean of bucket-level per-engineer rates) is disclosed as supplementary in §4.7 and §7 to surface methodology sensitivity. | A1: Report weighted rate only. A2: Report simple-mean rate only. A3: Report a per-bucket median instead of a mean. | The weighted rate describes the average productivity per engineer-bucket pair; the simple-mean rate gives each 2-week bucket equal weight regardless of the number of active engineers in that bucket. The two figures answer subtly different questions. In this repository the two rates diverge materially (weighted 0.56× Baseline vs. simple-mean 1.18× Baseline), so presenting only one would conceal the sensitivity. A3 discards the magnitude information that the mean preserves. | Dual reporting also doubles the Activity 7 row count in the Acceleration Curve and Risk Assessment. The two figures can point in opposite directions, which may confuse non-technical readers who expect a single "productivity multiplier" number; both are tagged High confidence, and the divergence itself is documented as a Medium-severity confounder in `acceleration-report.md` §8.3. |
| D014 | Aggregate Activities 2 and 5 by calendar quarter (Q1 = Jan–Mar, Q2 = Apr–Jun, Q3 = Jul–Sep, Q4 = Oct–Dec). A quarter is counted as "touched" by a phase if any day of the phase overlaps it. Baseline covers 75 quarters; Ramp-Up touches 2; Steady State touches 4; After (Ramp-Up + Steady State) touches 5. | A1: Fiscal quarters (offset by month). A2: 12-week rolling windows aligned to the Tool Introduction Date. A3: Quarter-year labels counted by unique label regardless of phase overlap (may attribute a single quarter to multiple phases). | The user prompt does not specify a calendar convention; calendar quarters are the most widely understood default and are stable across geographies. Counting quarters touched (rather than fully contained) captures partial-quarter phases without collapsing them; this keeps Activities 2 and 5 interpretable when a phase ends mid-quarter. A3 (shared-label attribution) would double-count quarters that straddle a phase boundary. | Small phases can touch multiple quarter labels and dilute the per-quarter rate. For Activity 5 the Ramp-Up phase touches 2 quarters with 0 doc creations (rate 0), while Steady State touches 4 quarters with 7 creations (rate 1.75). The Acceleration Curve row for Activity 5 therefore reads 0.00 / 1.75, which can be over-interpreted. Documented in `acceleration-report.md` §3.5. |
| D015 | Apply an extraction-timestamp ceiling to the Activity 5 After invocation: the `--before=${EXTRACT_TS}` bound caps the file-creation query at the recorded extraction timestamp (`2026-04-22T00:50:25+00:00`) so that commits made after extraction (e.g., the commit that lands this report itself) cannot inflate the count on subsequent re-runs. No ceiling is applied to other activities because their Baseline/After counts are already frozen relative to the fixed `--before=2025-06-19` / `--after=2025-06-19` bounds and the latest commit at extraction time. | A1: No extraction-timestamp ceiling on any activity (future commits will silently modify Activity 5 After counts on re-run). A2: Apply the ceiling uniformly to all activities (extra complexity for activities not at risk of post-extraction drift). A3: Use `HEAD` at extraction time instead of a timestamp ceiling (becomes stale as `HEAD` advances). | Activity 5 (Documentation creation) is the most exposed to post-extraction drift because the deliverables of this very task (`acceleration-report.md`, `decision-log.md`, `acceleration-report.html`) are themselves `.md` / `.html` file-creation events that will appear on re-run. Pinning the After invocation to the recorded extraction timestamp preserves the exact Activity 5 After value on any future re-execution of R6.1 against the same ref graph. Other activities use commit-count or branch-ref queries that are less susceptible to self-inflation because they are bounded by the Tool Introduction Date and the extraction-time commit count is stable across re-runs. | If the extraction timestamp is recorded incorrectly (e.g., with a timezone mismatch), the Activity 5 After count drifts by the size of the window. The timestamp is captured once via `date --iso-8601=seconds --utc` (R0.1) and reused verbatim throughout the report. Documented in the R6.1 code block header in `acceleration-report.md` §10. |
| D016 | Render the Slide 3 (Methodology Overview) extraction pipeline in `acceleration-report.html` as a Mermaid 11.4.0 `flowchart LR` inside `<pre class="mermaid">` per AAP §0.4.3 ("Methodology flow (reveal.js only): Mermaid flowchart LR") and per the Executive Presentation project rule (AAP §0.10.3). Retain the Slide 8 (Acceleration Curve) styled `<table>` as the temporal breakdown visualization per AAP §0.4.3 Option B ("styled HTML/Markdown table depending on Mermaid 11.4.0 feature availability"). Load Mermaid 11.4.0 via `https://cdn.jsdelivr.net/npm/mermaid@11.4.0/dist/mermaid.min.js` with Subresource Integrity hash `sha384-Wm9qzEgq4j1jEnuFK2FxKTlwuhbV2QqtGhcchvjDoKxeJ7WWAW7fysBq+1s6myfX` and `crossorigin="anonymous"`. Initialize with `mermaid.initialize({ startOnLoad: false, theme: 'base', securityLevel: 'strict', themeVariables: { primaryColor: '#F2F0FE', primaryTextColor: '#333333', primaryBorderColor: '#5B39F3', lineColor: '#999999', secondaryColor: '#F4EFF6', tertiaryColor: '#F4EFF6', fontFamily: "'Inter', system-ui, sans-serif" } })`. Call `await window.mermaid.run()` inside `renderVisuals()` AFTER `document.fonts.ready` resolves and BEFORE `window.lucide.createIcons()`, and invoke `renderVisuals()` from the reveal.js `ready` handler and from every `slidechanged` event, matching the invocation protocol AAP §0.10.3 prescribes. Separately, the AAP §0.4.3 "Commit velocity timeline" bullet — a line chart of monthly non-merge commits with a vertical marker at the Tool Introduction Date — is explicitly NOT rendered as a dedicated slide; the underlying temporal breakdown is captured via the Slide 8 Acceleration Curve table (Baseline / Ramp-Up / Steady State columns for Activity 7), which preserves data completeness while keeping the slide count at 16 within the [12, 18] band mandated by AAP §0.10.3. | A1: Retain the prior HTML/CSS `.flow-diagram` substitute for Slide 3 and continue omitting Mermaid entirely (the original D016 position prior to Checkpoint 4 QA remediation). A2: Render Slide 3 as Mermaid AND render Slide 8 as a Mermaid `xychart-beta` (AAP §0.4.3 Option A) to exercise the runtime on two slides. A3: Adopt Mermaid for Slide 3, retain table for Slide 8, AND add a dedicated commit velocity timeline slide (Mermaid `xychart-beta` of monthly non-merge commits) to satisfy the AAP §0.4.3 "Commit velocity timeline" bullet literally. A4: Render Slide 3 as a static SVG asset embedded inline. | Checkpoint 4 QA Issue #3 (MINOR) recorded the absence of Mermaid 11.4.0 as a deviation from AAP §0.6.1 (dependency inventory) and AAP §0.10.3 (Executive Presentation project rule); the remediation is to adopt Mermaid for Slide 3, which is the only AAP-catalogued Mermaid-native visualization (§0.4.3 "Methodology flow"). The font-metric race that motivated the original omission is mitigated by two concrete measures: (i) `await document.fonts.ready` in `renderVisuals()` blocks Mermaid rendering until the browser's FontFaceSet promise resolves, eliminating the reflow-after-font-load class of instability; (ii) the explicit `fontFamily: "'Inter', system-ui, sans-serif"` theme variable pins Mermaid's label font to the same family that the surrounding slide content uses, so Mermaid does not fall back to its default `--mermaid-font-family` and reflow when Inter arrives asynchronously. Slide 8 retains the styled `<table>` because AAP §0.4.3 Option B explicitly permits a styled HTML table in place of a Mermaid `xychart-beta`, and because the 9-row × 5-column temporal table provides higher information density than an `xychart-beta` at 1920×1080 reveal.js resolution — A2 would double-occupy screen real estate for the same data. A3 (dedicated timeline slide) would raise the slide count from 16 to 17, which is still within the [12, 18] band but would require re-authoring the divider distribution and would duplicate data already present in the Acceleration Curve Activity 7 rows; the timeline omission is therefore documented here as an explicit choice rather than implemented. A4 (static inline SVG) would require hand-authored path geometry that cannot be regenerated deterministically from the decision-log commands, violating the Explainability project rule's regeneration-from-rationale expectation. This decision reverses the prior D016 position (A1); the deviation is documented per the Explainability project rule ("Any deviation from a literal or obvious interpretation of the requirements MUST have an explicit entry in the decision log"). | Mermaid 11.4.0 adds approximately 500 KB of JavaScript to the initial page weight; this is an acceptable cost for a 1920×1080 executive presentation served from CDN to a LAN or broadband reviewer audience. The `startOnLoad: false` configuration means that a reveal.js initialization failure that prevents `renderVisuals()` from executing will leave the `<pre class="mermaid">` block unrendered as raw text (degraded but readable); the try/catch wrapper around `mermaid.run()` in `renderVisuals()` and the `if (window.mermaid && ...)` existence check prevent a Mermaid load failure from blocking subsequent Lucide icon rendering. The `securityLevel: 'strict'` setting disables user-supplied HTML within diagram labels, which is acceptable because the Slide 3 diagram text is static and authored in this repository. The commit velocity timeline omission (the AAP §0.4.3 second Mermaid-catalogued diagram) means that a reader seeking a month-by-month commit curve must consult the Acceleration Curve Activity 7 row or re-run R8.1 from `acceleration-report.md` §10; the risk is a small loss of visual immediacy, not a loss of data availability. Accessibility reviewers must confirm that the Mermaid-rendered inline SVG on Slide 3 exposes the `aria-label="Extraction pipeline flowchart"` on the `<pre class="mermaid">` container (which Mermaid preserves as the parent element's attribute after rendering). If a future reviewer chooses to enable A3 (dedicated timeline slide), the slide-count budget allows up to 2 additional slides before exceeding the AAP §0.10.3 upper bound of 18, and the `renderVisuals()` / `mermaid.run()` infrastructure is already present so no further runtime integration is required. |

---

## Bidirectional Traceability Matrix

### Decision → Report Section(s)

This table maps each Decision ID to the `acceleration-report.md` section(s) and `acceleration-report.html` slide(s) where the decision applies. Every decision listed in the table above appears in at least one row below.

| Decision ID | Report Sections Affected |
|-------------|--------------------------|
| D001 | §3.1 (Tool Introduction Date), §3.6 (Known Biases and Confounders), §7 (Acceleration Curve — phase partition anchor), §8.3 (Confounding Factors — anchor ambiguity row), §10 (Reproducibility Appendix R1.1–R1.3); `acceleration-report.html` Slide 3 (Methodology Overview) |
| D002 | §3.2 (Activities 3, 6, 7 bullets), §3.6 (Known Biases — bot activity), §4.3 (Activity 3), §4.6 (Activity 6 — bot-filtered ratio), §4.7 (Activity 7), §5 (Traceability rows T4.x, T7.x, T8.x), §6.1 (Activity 3 per-engineer), §6.2 (Activity 7 per-engineer), §8.3 (Confounding Factors — bot row), §10 (R4.1, R7.1, R8.1, R9.1, R9.2) |
| D003 | §3.2 (all 2-week activities), §3.5 (Window Alignment), §4.1, §4.3, §4.4, §4.7 (all 2-week rate lines), §5 (all T-rows with `2wk` units), §7 (bucket counts); `acceleration-report.html` Slide 3 (Methodology Overview), Slide 8 (Acceleration Curve) |
| D004 | §3.2 (Activities 3, 6, 7 bullets), §4.3 (Activity 3 — merges), §4.6 (Activity 6 — `--no-merges` denominator), §4.7 (Activity 7 — non-merges), §5 (rows T4.x, T7.x, T8.x), §10 (R4.1, R7.1, R8.1) |
| D005 | §3.2 (Activity 1 bullet), §4.1 (Activity 1), §5 (rows T2.1–T2.5), §8.3 (Confounding Factors — branch-naming row), §10 (R2.1, R2.2) |
| D006 | §3.2 (Activity 2 bullet), §4.2 (Activity 2), §5 (rows T3.1–T3.2), §8.2 (Insufficient-Signal Gaps), §9.2 (Proxy Limitations), §10 (R3.1) |
| D007 | §3.2 (Activity 6 bullet), §4.6 (Activity 6), §5 (rows T7.1–T7.8), §8.1 (Medium-confidence activities row), §10 (R7.1) |
| D008 | §6 preamble (anonymization rule), §6.1 (Activity 3 per-engineer), §6.2 (Activity 7 per-engineer), §10 (R9.2); `acceleration-report.html` Slide 10 (Engineers A/B/C… — Activity 3), Slide 11 (Engineers A/B/C… — Activity 7) |
| D009 | §1 (Executive Summary — Activity 2 line), §4.2 (Activity 2 all legs), §4.7 (Activity 7 Engineer A multiplier), §5 (all rows with `Status = Insufficient signal`), §7 (Activity 2 row), §8.2 (Insufficient-Signal Gaps), §9.1 (Data Gaps — explicit scope boundary) |
| D010 | §3.4 (Temporal Segmentation), §7 (Acceleration Curve), §9.1 (Data Gaps); `acceleration-report.html` Slide 8 (Acceleration Curve) |
| D011 | §3.2 (Activity 3 bullet — references `master`), §3.6 (Branch rename history bias), §4.1 (Activity 1 fallback), §4.3 (Activity 3), §5 (rows T2.1–T2.5, T4.1–T4.5), §8.3 (Confounding Factors — merge-author vs PR-author row adjacency), §10 (R2.2, R4.1) |
| D012 | §1 (Executive Summary — Activity 6 line), §3.2 (Activity 6 bullet), §4.6 (Activity 6 both-ratios table), §5 (rows T7.1–T7.8), §7 (Activity 6 non-bot and unfiltered rows), §8.3 (Confounding Factors — bot activity row), §10 (R7.1) |
| D013 | §1 (Executive Summary — Activity 7 line), §3.2 (Activity 7 bullet), §4.7 (Activity 7 weighted and simple-mean lines), §5 (rows T8.1–T8.8), §7 (Activity 7 weighted and simple-mean rows), §8.3 (Confounding Factors — simple-mean vs weighted row), §10 (R8.1) |
| D014 | §3.4 (Temporal Segmentation — quarter-count column), §3.5 (Window Alignment — calendar quarter definition), §4.2 (Activity 2), §4.5 (Activity 5), §5 (rows T3.1–T3.2, T6.1–T6.5), §7 (Activity 2, Activity 5 rows), §10 (R3.1, R6.1) |
| D015 | §2 (Environment Verification — Extraction timestamp row), §3.2 (Activity 5 bullet, implicit), §4.5 (Activity 5 After invocation), §5 (rows T6.2, T6.3, T6.5), §10 (R0.1, R6.1) |
| D016 | `acceleration-report.html` Slide 3 (Methodology Overview — Mermaid 11.4.0 `flowchart LR` inside `<pre class="mermaid">`, adopted per AAP §0.4.3 "Methodology flow" direction); `acceleration-report.html` Slide 8 (Acceleration Curve — styled `<table>` retained in place of Mermaid `xychart-beta`, AAP §0.4.3 Option B chosen over Option A); `acceleration-report.html` `<body>` script-tag block (addition of Mermaid 11.4.0 CDN `<script>` with SRI hash `sha384-Wm9qzEgq4j1jEnuFK2FxKTlwuhbV2QqtGhcchvjDoKxeJ7WWAW7fysBq+1s6myfX` and `crossorigin="anonymous"`, `mermaid.initialize({ startOnLoad: false, theme: 'base', securityLevel: 'strict', themeVariables: {...} })`, `await window.mermaid.run()` inside `renderVisuals()` after `document.fonts.ready`); `acceleration-report.html` `<style>` block (addition of `.mermaid` and `.mermaid svg` CSS rules); `acceleration-report.html` absence of a dedicated commit velocity timeline slide (the AAP §0.4.3 second Mermaid-catalogued diagram — monthly non-merge commits with TID marker — is explicitly not rendered; equivalent temporal data is available on Slide 8 Acceleration Curve Activity 7 rows). Coordinates with the Executive Presentation project rule captured in `acceleration-report.md` §0.10.3 and with AAP §0.6.1 dependency inventory |

### Report Section → Decision IDs

This table reverses the mapping. Every major section of `acceleration-report.md` appears in at least one row, and every Decision ID above appears in at least one row across the two tables combined.

| Report Section | Decision IDs Applied |
|----------------|----------------------|
| §1 (Executive Summary) | D001, D002, D004, D005, D006, D008, D009, D012, D013 |
| §2 (Environment Verification) | D015 |
| §3 (Methodology — overview) | D001, D002, D003, D004, D005, D006, D007, D009, D010, D011, D012, D013, D014, D015 |
| §3.1 (Tool Introduction Date) | D001 |
| §3.2 (Per-Activity Extraction Approach) | D001, D002, D004, D005, D006, D007, D011, D012, D013, D015 |
| §3.3 (Confidence Rationale) | D005, D007 (Medium-confidence tags), D009 (Insufficient-signal row) |
| §3.4 (Temporal Segmentation) | D001, D010, D014 |
| §3.5 (Window Alignment) | D003, D014 |
| §3.6 (Known Biases and Confounders) | D001, D002, D005, D011 |
| §4.1 (Activity 1 — Requirements Throughput) | D003, D005, D011 |
| §4.2 (Activity 2 — Architecture & Design) | D006, D009, D014 |
| §4.3 (Activity 3 — Code Generation) | D002, D003, D004, D011 |
| §4.4 (Activity 4 — Test Creation) | D003 |
| §4.5 (Activity 5 — Documentation) | D014, D015 |
| §4.6 (Activity 6 — Defect Response) | D002, D004, D007, D012 |
| §4.7 (Activity 7 — Commit Throughput) | D002, D003, D004, D008, D013 |
| §5 (Requirements Traceability Matrix) | D001, D002, D003, D004, D005, D006, D007, D008, D009, D011, D012, D013, D014, D015 |
| §6 (Per-Engineer Acceleration — preamble) | D008 |
| §6.1 (Activity 3 per-engineer) | D002, D004, D008, D011 |
| §6.2 (Activity 7 per-engineer) | D002, D004, D008, D009, D013 |
| §7 (Acceleration Curve) | D003, D006, D009, D010, D012, D013, D014 |
| §8 (Risk Assessment — overview) | D001, D002, D005, D006, D007, D009, D011, D012, D013 |
| §8.1 (Low-Confidence / Medium-Confidence Activities) | D005, D007 |
| §8.2 (Insufficient-Signal Gaps) | D006, D009 |
| §8.3 (Confounding Factors) | D001, D002, D004, D005, D011, D012, D013 |
| §9.1 (Data Gaps) | D009, D010 |
| §9.2 (Proxy Limitations) | D005, D006, D007 |
| §9.3 (What This Analysis Cannot Determine) | D001 (anchor ambiguity), D010 (phase-collapse scenario) |
| §10 (Reproducibility Appendix — R0.x) | D015 (R0.1 extraction timestamp) |
| §10 (Reproducibility Appendix — R1.x) | D001 |
| §10 (Reproducibility Appendix — R2.x) | D005, D011 |
| §10 (Reproducibility Appendix — R3.x) | D006, D014 |
| §10 (Reproducibility Appendix — R4.x) | D002, D004, D011 |
| §10 (Reproducibility Appendix — R5.x) | D003 |
| §10 (Reproducibility Appendix — R6.x) | D014, D015 |
| §10 (Reproducibility Appendix — R7.x) | D002, D004, D007, D012 |
| §10 (Reproducibility Appendix — R8.x) | D002, D003, D004, D013 |
| §10 (Reproducibility Appendix — R9.x) | D002, D008 |
| `acceleration-report.html` Slide 3 (Methodology Overview) | D001, D003, D016 |
| `acceleration-report.html` Slide 8 (Acceleration Curve) | D003, D010, D012, D013, D016 |
| `acceleration-report.html` Slides 10, 11 (Engineers A/B/C…) | D002, D008, D011 |
| `acceleration-report.html` `<head>` + script-tag block (CDN dependency pinning) | D016 |

Coverage check: every Decision ID (D001 through D016) appears in at least one row of both tables, and every major report section listed above appears in at least one row. Zero gaps.

---

## Engineer Anonymization Mapping

This section documents the **rule** used to map real author identities to pseudonyms `Engineer A`, `Engineer B`, … for the per-engineer breakdowns in `acceleration-report.md` §6.1 and §6.2. The rule is deterministic and regenerable; the actual author identities are not recorded here, consistent with the user prompt's instruction that the decision log documents the rule, not the identities.

### Regeneration Command

The After-period non-bot non-merge commit ranking that drives the pseudonym assignment is generated by running the following read-only commands against the repository at the same extraction timestamp recorded in `acceleration-report.md` §2:

```bash
# Rank non-bot human authors by After-period non-merge commit volume (descending).
git shortlog -sne --no-merges --since=2025-06-19 \
  | grep -vE '(\[bot\]|jenkins-release-bot|release-bot)' \
  | sort -rn \
  | head -20
```

The top 20 authors from this command form the pool from which Activity 7 §6.2 draws its 10-row breakdown. A comparable command with `--merges --first-parent master` rather than `--no-merges` produces the Activity 3 §6.1 ordering (only 2 engineers produced non-bot merges in the After period, so `Engineer A` and `Engineer B` are the only two pseudonyms assigned for Activity 3). The regeneration command above is exactly `R9.2` in `acceleration-report.md` §10; no additional data source or tool is required.

### Assignment Rule

- The author at rank 1 (highest After-period non-bot non-merge commit volume) is assigned `Engineer A`.
- The author at rank 2 is assigned `Engineer B`.
- Each subsequent rank receives the next letter of the Latin alphabet, in order.
- The mapping is stable across the report and the executive presentation: any row labelled `Engineer A` in §6.1, §6.2, or `acceleration-report.html` Slides 10 and 11 refers to the same underlying author identity.
- The mapping is regenerable at any future time by re-running the command above against the same repository state. A changed ref graph (new commits, removed commits, rebased history) will produce a different mapping; the mapping is therefore a function of the extraction timestamp recorded in §2.

### Author-Category Attribution

The decision log records one factual, non-identifying note about author categories present in the ranking:

- `Engineer A` in the Activity 7 §6.2 ranking has zero Baseline non-merge commits. The associated multiplier is therefore rendered `Insufficient signal — no Baseline commits (division by zero)` per D009. This outcome reflects a first-time contributor whose activity falls entirely within the After period; it is a legitimate artefact of a lifetime-versus-phase comparison and not a data-quality problem.
- Activity 3 §6.1 `Engineer A` and `Engineer B` are maintainers who perform merges on `master`; per §6.1 footnote, this is a maintainer-activity proxy rather than a direct PR-authorship proxy. The pseudonym letters in §6.1 and §6.2 are independent (they rank by different ref sets) and do not necessarily map to the same underlying person.

No real author names, emails, or other identifying attributes appear in this log.

---
