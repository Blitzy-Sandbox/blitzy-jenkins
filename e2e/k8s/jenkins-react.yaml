---
# Jenkins React instance — React 19 UI enabled via JENKINS_REACT_ENABLED env var.
# Used as the target for visual regression comparison against baseline.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jenkins-home-react
  namespace: jenkins-e2e
  labels:
    app: jenkins-react
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: jenkins-react
  namespace: jenkins-e2e
  labels:
    app: jenkins-react
    instance-type: react
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jenkins-react
  template:
    metadata:
      labels:
        app: jenkins-react
        instance-type: react
    spec:
      securityContext:
        fsGroup: 1000
      initContainers:
        # Copy seed data from shared init volume if available
        - name: init-home
          image: jenkins/jenkins:lts-jdk21
          command:
            - /bin/sh
            - -c
            - |
              if [ ! -f /var/jenkins_home/config.xml ]; then
                echo "Fresh JENKINS_HOME — init-job will populate it."
              fi
          volumeMounts:
            - name: jenkins-home
              mountPath: /var/jenkins_home
      containers:
        - name: jenkins
          image: jenkins/jenkins:lts-jdk21
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
            - name: agent
              containerPort: 50000
              protocol: TCP
          env:
            - name: JENKINS_OPTS
              value: "--prefix=/jenkins"
            - name: JENKINS_REACT_ENABLED
              value: "true"
            - name: JAVA_OPTS
              value: >-
                -Djenkins.install.runSetupWizard=false
                -Dhudson.security.csrf.GlobalCrumbIssuerConfiguration.DISABLE_CSRF_PROTECTION=true
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: "2"
              memory: 2Gi
          readinessProbe:
            httpGet:
              path: /jenkins/login
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 12
          livenessProbe:
            httpGet:
              path: /jenkins/login
              port: 8080
            initialDelaySeconds: 120
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 5
          volumeMounts:
            - name: jenkins-home
              mountPath: /var/jenkins_home
      volumes:
        - name: jenkins-home
          persistentVolumeClaim:
            claimName: jenkins-home-react
---
apiVersion: v1
kind: Service
metadata:
  name: jenkins-react
  namespace: jenkins-e2e
  labels:
    app: jenkins-react
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: jenkins-react
